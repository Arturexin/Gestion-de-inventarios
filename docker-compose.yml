
services:
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile.backend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - mysql
  #     - redis
  #   environment:
  #     PORT: 3000
  #     DB_HOST: mysql
  #     DB_PORT: 3306
  #     DB_USER: root
  #     DB_PASSWORD: Luter123.$-
  #     DB_NAME: control_asistencia
  #     SAL_ROUNDS: 10
  #     SECRET_JWT_KEY: esta-es-la-clave-secreta-del-token-lo-mas-grande-posible
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #     KEY_REDIS_EXPIRY: 43200
  #     ACCESS_TOKEN_EXPIRY: 900
  #     REFRESH_TOKEN_EXPIRY: 604800
  #   restart: always
  #   networks:
  #     - app-network

  # nginx:
  #   build:
  #     context: .
  #     dockerfile: ./nginx/Dockerfile.nginx
  #   image: control-asistencia-nginx
  #   container_name: nginx
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - backend
  #   networks:
  #     - app-network

  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Luter123.$-
      MYSQL_DATABASE: karpovick_db
    ports:
      - "3306:3306"   # 👈 expone MySQL a tu máquina
    volumes:
      - mysqldata:/var/lib/mysql
      - ./databases/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network

  # redis:
  #   image: redis:7
  #   restart: always
  #   volumes:
  #     - redisdata:/data
  #   networks:
  #     - app-network

volumes:
  mysqldata:
  # redisdata:

networks:
  app-network:
    driver: bridge
